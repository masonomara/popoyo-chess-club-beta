Read `docs/schema.sql` and `docs/01-goal.md` before writing anything. Understand every table, constraint, and user role.

## Task

Write realistic sample data to `docs/seed.sql` that covers the full shape of the application.

- At least one row per user role described in `docs/01-goal.md`
- Enough rows per table to make the app feel real — not 1 row, but a working state
- All data must respect every constraint in `schema.sql` — foreign keys, NOT NULL, UNIQUE, check constraints
- Hardcoded UUIDs so foreign key relationships are explicit and auditable
- Realistic values — not `test1`, `foo`, `bar`

Begin the file with:
```sql
-- Seed data for [project name]. Generated by /seed.
-- Run in the Supabase SQL Editor after schema.sql.
```

After writing the file, list every table seeded and how many rows were inserted.
