Read `goal.md` thoroughly. Understand the context, users, flows, screens, success criteria, and key mutations.

Then produce a concise plan that covers:
1. The screens we're building and which is the first (read path target)
2. The primary mutation (write path target)
3. Any secondary surfaces to defer until after the milestone
4. Anything unclear that needs a decision before writing schema

Keep it short. This is a 60-minute build — cut scope aggressively if needed.
